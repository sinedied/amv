Add date prefix to filenames:
- Add current date in YYYY-MM-DD format at the beginning
- Separate date from original filename with a hyphen
- Keep original filename structure
- Keep file extensions unchanged